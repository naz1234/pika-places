{
  "name": "pika-places",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A public, shared Malaysia place wishlist for Cloudflare Pages, D1 and R2.",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "dev": "wrangler pages dev public --config wrangler.local.jsonc",
    "db:local": "wrangler d1 execute DB --local --config wrangler.local.jsonc --file migrations/0001_initial.sql",
    "check": "npm test && npm run build",
    "test:runtime": "npm run build && node scripts/runtime-smoke.mjs"
  },
  "devDependencies": {
    "fake-indexeddb": "6.2.5",
    "wrangler": "4.127.0"
  }
}
